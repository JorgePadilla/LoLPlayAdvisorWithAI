<div class="container mx-auto px-4 py-8">
  <div class="bg-white rounded-lg shadow-lg p-6">
    <div class="mb-6">
      <h1 class="text-3xl font-bold text-gray-800 mb-2">
        🎯 Select Player to Analyze
      </h1>
      <p class="text-gray-600">
        Your replay "<strong><%= @replay.filename %></strong>" contains data for all 10 players. Choose which player's performance you want to analyze.
      </p>
    </div>

    <div class="mb-6 p-4 bg-blue-50 border border-blue-200 rounded-lg">
      <div class="flex items-center">
        <div class="text-blue-600 mr-3">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
        </div>
        <div>
          <h3 class="font-semibold text-blue-800">Why do I need to select a player?</h3>
          <p class="text-blue-700 text-sm mt-1">
            Replay files contain data for all 10 players, but don't specify who downloaded the replay. 
            Select the player whose performance you want to analyze.
          </p>
        </div>
      </div>
    </div>

    <%= form_with url: update_player_selection_replay_path(@replay), method: :patch, local: true, class: "space-y-4" do |form| %>
      <div class="grid gap-4">
        <% @all_players.each_with_index do |player, index| %>
          <div class="border border-gray-200 rounded-lg p-4 hover:border-blue-300 transition-colors">
            <label class="flex items-center cursor-pointer">
              <%= form.radio_button :player_index, index, class: "mr-4 text-blue-600" %>
              
              <div class="flex-1">
                <div class="flex items-center justify-between">
                  <div class="flex items-center space-x-4">
                    <!-- Champion Image -->
                    <div class="w-12 h-12 bg-gray-100 rounded-lg flex items-center justify-center">
                      <img src="<%= AccurateRoflParser.get_champion_image_url(player[:champion]) %>" 
                           alt="<%= player[:champion] %>" 
                           class="w-10 h-10 rounded"
                           onerror="this.style.display='none'; this.nextElementSibling.style.display='block';">
                      <span class="hidden text-xs text-gray-500"><%= player[:champion] %></span>
                    </div>
                    
                    <!-- Player Info -->
                    <div>
                      <div class="font-semibold text-gray-800">
                        <%= player[:summoner_name] %>#<%= player[:summoner_tag] %>
                      </div>
                      <div class="text-sm text-gray-600">
                        <%= player[:champion] %>
                      </div>
                    </div>
                  </div>
                  
                  <!-- Stats -->
                  <div class="flex items-center space-x-6 text-sm">
                    <!-- Team -->
                    <div class="text-center">
                      <div class="text-xs text-gray-500">Team</div>
                      <div class="font-medium <%= player[:team] == '100' ? 'text-blue-600' : 'text-red-600' %>">
                        <%= player[:team] == '100' ? 'Blue' : 'Red' %>
                      </div>
                    </div>
                    
                    <!-- KDA -->
                    <div class="text-center">
                      <div class="text-xs text-gray-500">KDA</div>
                      <div class="font-medium text-gray-800">
                        <%= player[:kda] %>
                      </div>
                    </div>
                    
                    <!-- Gold -->
                    <div class="text-center">
                      <div class="text-xs text-gray-500">Gold</div>
                      <div class="font-medium text-yellow-600">
                        <%= number_with_delimiter(player[:gold]) %>
                      </div>
                    </div>
                    
                    <!-- CS -->
                    <div class="text-center">
                      <div class="text-xs text-gray-500">CS</div>
                      <div class="font-medium text-gray-600">
                        <%= player[:cs] %>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            </label>
          </div>
        <% end %>
      </div>

      <div class="flex justify-between items-center pt-6 border-t border-gray-200">
        <%= link_to "← Back to Replay", replay_path(@replay), 
            class: "px-4 py-2 text-gray-600 hover:text-gray-800 transition-colors" %>
        
        <%= form.submit "Analyze Selected Player", 
            class: "px-6 py-3 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors font-medium" %>
      </div>
    <% end %>
  </div>
</div>
